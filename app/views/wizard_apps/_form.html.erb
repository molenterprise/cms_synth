<%= form_for(@wizard_app) do |f| %>
  <% if @wizard_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wizard_app.errors.count, "error") %> prohibited this wizard_app from being saved:</h2>

      <ul>
      <% @wizard_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
  	<%= f.label :ontology_id %><br>
    <%= f.select :ontology_id, Ontology.all.collect { |o| [ o.name, o.id ] } %><br>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-large btn-primary"%>
  </div>
<% end %>
